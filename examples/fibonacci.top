package main

|- Naive recursive Fibonacci for quick experiments

@cache
def fib(n: Int): Int {
  if n <= 1 then top n
  else top fib(n - 1) + fib(n - 2)
}

def main(): Int {
  println("fib(10) = " ++ show(fib(10)))
  top 42
}
